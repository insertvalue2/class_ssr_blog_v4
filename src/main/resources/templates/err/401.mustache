{{> layout/header}}

<div class="container my-3">
    <h1>Unauthorized 401</h1>
    <hr>
    <h4>{{msg}}</h4>
    <div class="mt-3">
        <a href="/login-form" class="btn btn-primary">로그인</a>
        <a href="/" class="btn btn-secondary">메인으로</a>
    </div>
</div>

{{> layout/footer}}